# Buffer tracking

Implemented in `dirk_render_utils::graph`: whole-buffer declarations, import boundary states, usage validation, dependencies, and first-read checks. Byte-range tracking is deferred.
